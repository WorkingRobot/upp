#pragma once

#include "FMovieSceneEvalTemplatePtr.h"

namespace upp::Objects {
    // Not actually the same type, but it's the same serialization
    using FMovieSceneTrackImplementationPtr = FMovieSceneEvalTemplatePtr;
}